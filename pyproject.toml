[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hand-bone-measurements-app"
version = "0.1.0"
description = "Hand bone measurements Streamlit app"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["src"]
